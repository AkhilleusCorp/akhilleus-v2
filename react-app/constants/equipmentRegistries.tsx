import IndexedArray from "app/utils/interfaces/IndexedArray.tsx";

const statuses: IndexedArray = {
    'active': 'Active',
    'draft': 'Draft',
    'deactivated': 'Deactivated',
}

const equipmentRegistries = {
    status: statuses,
}

export default equipmentRegistries;