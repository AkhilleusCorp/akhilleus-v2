<header id="main-unlogged-header">
    <div class="menu">
        <ul class="inline">
            {% if app.user %}
            <li>
                <a href="/logout">Logout</a>
            </li>
            {% else %}
                <li>
                    <a href={{ path('website_authentication') }}>Username</a>
                </li>
                <li>
                    <a href={{ path('website_registration') }}>Registration</a>
                </li>
            {% endif %}
        </ul>
    </div>
</header>
